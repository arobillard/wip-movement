import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('Your cool new app', () => {
  it('checks for a use', () => {
    expect(App).to.hav.
  })
})